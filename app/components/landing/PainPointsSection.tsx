export default function PainPointsSection() {
  return (
    <section className="border-b border-slate-200 bg-white px-4 py-16 sm:px-6">
      <div className="mx-auto max-w-3xl text-center">
        <h2 className="text-2xl font-bold text-slate-900 sm:text-3xl">
          Location mistakes are expensive
        </h2>
        <p className="mt-6 text-left text-base leading-relaxed text-slate-600 sm:text-center">
          Opening a store is risky — studies show a large share of retail
          failures tie back to poor location choice. Manually checking
          competitors on Google Maps is slow and inconsistent; enterprise tools
          like Placer.ai cost thousands per year. Store Location Analyzer gives
          solo founders and small businesses a lightweight, map-first way to
          score sites in minutes.
        </p>
      </div>
    </section>
  );
}
